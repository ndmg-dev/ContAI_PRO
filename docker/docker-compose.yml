services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - ../.env
    volumes:
      - ../app:/app/app
      - ../main.py:/app/main.py
    environment:
      - FLASK_ENV=development
      - FLASK_APP=main.py
